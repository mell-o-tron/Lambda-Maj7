negOneToThe := lam (x) => {
    =(x, 0) ? 1 : *((-1), negOneToThe +(x, -1))
};

pi := lam (x) => {
    < (x, 1000) ? 
        +( pi(+(1, x)),
           *(-negOneToThe(x),
                /(10000000, 
                  + (*(2, x), -1)
                 )
            )
         ) : 0 
} ; * (pi 1, 4)
